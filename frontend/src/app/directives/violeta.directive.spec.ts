import { VioletaDirective } from './violeta.directive';

describe('VioletaDirective', () => {
  it('should create an instance', () => {
    const directive = new VioletaDirective();
    expect(directive).toBeTruthy();
  });
});
